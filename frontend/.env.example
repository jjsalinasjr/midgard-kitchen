# Server-side only — used by /api/token. NEVER exposed to the browser.
LIVEKIT_URL=wss://<your-project>.livekit.cloud
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=
ACCESS_CODE=

# Public — safe to expose to the browser (used by the client to connect).
NEXT_PUBLIC_SESSION_LIMIT_MIN=10
